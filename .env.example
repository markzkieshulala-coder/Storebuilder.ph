# Database
DATABASE_URL="postgresql://user:password@localhost:5432/storebuilder"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Website generation — IN-HOUSE engine + optional AI UNDERSTANDING.
# The website is always generated 100% in-process by the in-house engines.
# OPTIONALLY, OpenAI can be used for PROMPT UNDERSTANDING ONLY: it turns the user's
# prompt into a structured spec (nav, sections, content) which the in-house engine
# then renders. No HTML is ever sent to OpenAI — only the prompt comes back as a
# spec. Without this key (or on any error/quota issue), understanding falls back to
# the deterministic parser and the app keeps working.
#   - Requires an OpenAI account WITH billing/credits (a key with no quota returns
#     HTTP 429 and the system silently falls back).
OPENAI_API_KEY=""
AI_MODEL="gpt-4o-mini"        # understanding model (cheap + fast). gpt-4o for hardest prompts.
# OPENAI_BASE_URL="https://api.openai.com/v1"   # override for an OpenAI-compatible endpoint
# AI_TIMEOUT_MS="25000"

# PayMongo
PAYMONGO_SECRET_KEY="sk_test_your-paymongo-secret-key"
PAYMONGO_PUBLIC_KEY="pk_test_your-paymongo-public-key"

# Cloudflare
CLOUDFLARE_API_TOKEN="your-cloudflare-api-token"
CLOUDFLARE_ZONE_ID="your-cloudflare-zone-id"
CLOUDFLARE_ACCOUNT_ID="your-cloudflare-account-id"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_PAYMONGO_PUBLIC_KEY="pk_test_your-paymongo-public-key"

# Email (nodemailer)
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="your-app-password"
EMAIL_FROM="noreply@storebuilder.ph"

# USD to PHP conversion rate (update periodically)
USD_TO_PHP_RATE="57.5"

# Visuals — content-aware Pexels photos, globally unique per build.
# Each section is resolved from the EXACT content it shows (product/service names,
# niche, branding); a persistent ledger guarantees no image is ever reused across
# builds, even within the same niche. Without a key, every slot falls back to a
# branded CSS placeholder (site generation still works).
# Get a free key at https://www.pexels.com/api/ (create account → Your API key).
PEXELS_API_KEY=""
# Optional tuning:
PEXELS_BUDGET_MS="30000"      # total wall-clock budget for the whole image phase
PEXELS_TIMEOUT_MS="8000"      # per-search request timeout
